<div class='box-card-section'>
    <% data.bestSells.forEach(function(product){ %>
        <!--Comienza un item-->
            <div class="box-card">
                <div>
                    <div class='favourite'>
                        <% if (data.session.user) { %>
                            <div class='coverHeart' productId='<%= product.id %>' userid='<%= data.session.user.id %>'></div>
                        <% } else { %>
                            <div class='coverHeart' productId='<%= product.id %>'></div>
                        <% } %>

                        <% if (data.favorites[product.id]) { %>
                            <img class='heartInactive' src="/files/heartInactive.svg" alt="">
                            <img id='<%= product.id %>Heart' style='display:flex;' class='heart-out' src="/files/heartActive.svg" alt="">
                        <% } else { %>
                            <img class='heartInactive' src="/files/heartInactive.svg" alt="">
                            <img id='<%= product.id %>Heart' class='nodisplay heart-out' src="/files/heartActive.svg" alt="">
                        <% } %>
                    </div>
                    <a class='cardProductLink' href="/product/<%= product.id %>">
                        <div class='box-card-top'>
    
                            <div class='card-item-image'>
                                <figure>
                                    <img src="/img/products/<%= product.img %>" alt="">
                                </figure>
                            </div>
                            <div class='card-item-name'>
                                <span class='product-name'><%= Array.from(product.name).slice(0,64).join('')+'...' %></span>
                            </div>
                            <div class='card-item-description'>
                                <span><%= Array.from(product.description).slice(0,122).join('')+'...' %></span>
                            </div>
                            <div class='card-item-price'>
                                <span>$<%= product.sellPrice %></span>
                            </div>
                            <div class='card-item-valoration'>
                                <div>
                                    <% for( let index = 0; index < product.valoration; index++ ) { %>
                                        <img src="/files/star.svg" alt="">
                                    <% } %>
                                </div>
                            </div>
                        </div>
                    </a>
                    <div class='box-card-bottom'>
                        <span>Al carrito</span>
                    </div>
                </div>
            </div>
        <!--fin del item-->
    <% }); %>
</div>

